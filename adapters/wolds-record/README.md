# Wolds Record adapter

This adapter is the project-specific layer for the Wolds Record repository.

It should define the repository's `AGENTS.md` rules, source-of-truth paths, PRD task shape, organisation-scoping requirements, mandatory npm validation suite, branch policy, and release/review rules. Keep those constraints here rather than in the reusable workflow skills.
